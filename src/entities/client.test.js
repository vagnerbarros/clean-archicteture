describe('Client Endpoint', () => {

  it('client with invalid CPF', (done) => {
    expect(true).to.be(false);
    done();
  });

  it('client with invalid CEP', (done) => {
    expect(true).to.be(false);
    done();
  });

  it('client without CPF', (done) => {
    expect(true).to.be(false);
    done();
  });

  it('client without CEP', (done) => {
    expect(true).to.be(false);
    done();
  });
  
  it('client without Name', (done) => {
    expect(true).to.be(false);
    done();
  });
})